

import React from 'react';

const CarDetails = ({ car }) => (
  <div className="container mx-auto p-4">
    <h1 className="text-2xl font-bold mb-8 font-serif">{car.name}</h1>
    <div className="flex">
        
      {/* Display car details here */}
    </div>
  </div>
);

export default CarDetails;
